//- Все примеси в этом файле должны начинаться c имени блока (faq)

mixin faq(items)
	.faq__accordeon
		each item in items
			.faq__collapse
				.faq__collapse-header
					.faq__number!= item.num
					.faq__title!= item.title
					svg.faq__icon.svg
						use(xlink:href='img/svgSprite.svg#icon__arrow_down')
				.faq__collapse-item
					p!= item.info
